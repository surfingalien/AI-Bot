{
  "name": "surfingalien-proxy",
  "version": "2.0.0",
  "private": true,
  "description": "SurfingAlien AI proxy — one isolated brain per principal, sessions, scoped SSE, server-pushed reminders.",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
